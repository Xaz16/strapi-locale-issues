'use strict';

/**
 * locale-with-relation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::locale-with-relation.locale-with-relation');

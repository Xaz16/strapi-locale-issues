'use strict';

/**
 * locale-with-relation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::locale-with-relation.locale-with-relation');
